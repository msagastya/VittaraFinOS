# Plan — created 24 April 2026, 00:18
# Target: 7.8 → 9.8 / 10

**200 atomic tasks across 10 phases.**
Each task is a single, shippable unit of work.
Phases are ordered: bugs first → foundation → features → intelligence → world-class polish.

---

## SCORING TARGETS

| Dimension | Now | Target |
|---|---|---|
| Idea & Vision | 9.2 | 9.5 |
| Feature Depth | 9.0 | 9.8 |
| AI Intelligence | 7.5 | 9.8 |
| UX Smoothness | 7.0 | 9.8 |
| Navigation / IA | 6.5 | 9.5 |
| Personalization | 8.0 | 9.8 |
| Unified Experience | 7.8 | 9.8 |
| Security & Trust | 9.0 | 9.8 |
| Onboarding | 6.0 | 9.5 |
| Performance | 8.0 | 9.8 |
| Reliability | 7.5 | 9.8 |
| **Overall** | **7.8** | **9.8** |

---

## PHASE 0 — Critical Bugs (3 bugs, 18 tasks)
*These break trust on every session. Nothing else ships before these are done.*

---

### BUG-01 · Orientation Change (Fields clear, sheets fight, layout breaks)

**T-001** `account_wizard.dart` — Replace every bare `TextEditingController` field with `RestorableTextEditingController` so text survives orientation rebuild. Register all controllers with `registerForRestoration`.

**T-002** `transaction_wizard.dart` (QuickEntrySheet) — Same `RestorableTextEditingController` treatment for amount, description, notes, merchant, and tag fields.

**T-003** All investment wizard steps (`mf_search_step`, `mf_transaction_details_step`, `fd_wizard_screen`, `rd_wizard_screen`, `stocks_wizard`, `bonds_wizard`, `crypto_wizard`, `digital_gold_wizard`, `nps_wizard`, `pension_wizard`, `commodities_wizard`, `fo_wizard`) — Add `AutomaticKeepAliveClientMixin` + `wantKeepAlive = true` to each step's `State` class so wizard steps don't rebuild from scratch on orientation change.

**T-004** `account_wizard.dart`, all investment wizards — Store the current step index in a `RestorableInt` so the wizard page position survives rebuild.

**T-005** `QuickEntrySheet` — Wrap the sheet content in `OrientationBuilder`. On landscape phone (width > 600 and height < 500), switch to a 2-column form layout with amount/description on left and category/account on right. Max constrain to 560pt width (same as existing tablet constraint).

**T-006** `VoiceOverlayWidget.show()` — Call `SystemChrome.setPreferredOrientations([DeviceOrientation.portraitUp])` before showing, restore to `DeviceOrientation.values` after modal pops. This prevents voice overlay ever fighting orientation mid-session.

**T-007** `GlobalSearchOverlay` (`global_search_overlay.dart`) — Same orientation lock as T-006. Lock to portrait during overlay, restore on dismiss.

**T-008** All `showCupertinoModalPopup` callers that wrap form sheets — Add explicit `constraints: BoxConstraints(maxHeight: MediaQuery.of(context).size.height * 0.92)` to prevent sheets overflowing past screen height in landscape.

**T-009** `AccountsScreen._categoryPageController` — Move `PageController` creation into a getter that reads `_selectedCategoryIndex` so if the widget rebuilds after rotation, the controller initialises at the correct page rather than page 0.

**T-010** `InvestmentsScreen._categoryPageController` — Same fix as T-009. Save `_selectedCategoryIndex` to `PageStorageBucket` via `PageStorageKey` so it also survives pushes/pops not just orientation.

**T-011** `DashboardScreen` — Add `RestorationMixin` and `restorationId: 'dashboard'` to the state so the scroll position and any expanded widget state survive rotation.

**T-012** `LandscapeScaffold` / `landscape_split_view.dart` — Audit the landscape split view widget. Ensure the master-detail split reflows correctly when going portrait → landscape → portrait without the detail pane losing its state.

---

### BUG-02 · Voice Zombie (Mic stays alive 15+ min, stuck screen)

**T-013** `VoiceController` — Implement `WidgetsBindingObserver`. In `init()` call `WidgetsBinding.instance.addObserver(this)`. In `dispose()` call `WidgetsBinding.instance.removeObserver(this)`. Override `didChangeAppLifecycleState`: on `paused` or `detached` → call `cancel()` immediately.

**T-014** `VoiceController` — Add `Timer? _sessionTimeoutTimer` and `_resetSessionTimeout()` method. Every time `startListening()` or `_listenForAnswer()` is called, start/restart a 45-second timer. If it fires while state is not idle, call `cancel()`. Cancel the timer in `cancel()`, `confirm()`, and `dispose()`.

**T-015** `VoiceOverlayWidget.show()` — After the `showCupertinoModalPopup` future resolves (however the modal closed), check `if (voice.state != VoiceState.idle) voice.cancel()`. This guarantees the mic dies even if the barrier was tapped or the user back-gestured.

**T-016** `VoiceOverlayWidget` — The barrier `GestureDetector.onTap` currently only calls `voice.cancel()` when state is `idle` or `error`. Extend it to call `voice.cancel()` unconditionally on any barrier tap, then `Navigator.of(context).pop(null)`.

**T-017** `VoiceController` — Add `_stt.statusListener` callback. If STT reports `notListening` status while `_state` is still `listening`, transition to `idle` automatically so the UI never shows a listening indicator when STT has already stopped internally.

**T-018** `voice_overlay_widget.dart` — Add a visual "tap anywhere to dismiss" hint text at the very bottom of the overlay card (small, muted) so users know how to escape the overlay rather than being stuck.

---

### BUG-03 · Header Strip Out of Sync With Content Swipe

**T-019** `InvestmentsScreen` — Replace the hardcoded `const tabWidth = 90.0` in both the tab-tap handler and `onPageChanged` handler with a `List<GlobalKey> _tabKeys` approach: assign one `GlobalKey` per tab pill, measure each tab's actual `RenderBox.localToGlobal` offset after the first frame, store in `List<double> _tabPositions`. Use these real positions in `_tabStripController.animateTo()`.

**T-020** `InvestmentsScreen` — Add `_categoryPageController.addListener(_syncHeaderOnDrag)` in `initState`. `_syncHeaderOnDrag` reads `_categoryPageController.page` (a double during drag), linearly interpolates between `_tabPositions[page.floor()]` and `_tabPositions[page.ceil()]`, then calls `_tabStripController.jumpTo()` (not `animateTo` — jumpTo is frame-synchronous during drag).

**T-021** `AccountsScreen` — Apply the same `GlobalKey`-measured tab position pattern + continuous drag listener as T-019/T-020. The header `ListView` must track the `PageView` in real-time, not just on snap.

**T-022** `LendingBorrowingScreen` — Audit for same header/content sync issue. Apply the same fix (GlobalKey + continuous page listener) if the screen uses separate scroll controllers for header and content.

---

## PHASE 1 — Navigation & Information Architecture (22 tasks)

---

### NAV-01 · Universal Command Bar

**T-023** Create `lib/ui/widgets/command_bar.dart` — A full-screen modal (similar to global search overlay) with a search field at top. Sections: Recent Actions, Quick Add, Navigate To, Smart Suggestions. Show via a new static `CommandBar.show(context)` method.

**T-024** `CommandBar` — Wire "Quick Add" section: rows for "Add Expense", "Add Income", "Add Transfer", "New Investment", "New Goal", "New Budget". Each opens the relevant wizard/sheet on tap.

**T-025** `CommandBar` — Wire "Navigate To" section: dynamically generated from `VoiceNavigator._map` labels — all 13 nav targets as tappable rows with their screen icons. Tapping navigates and dismisses the bar.

**T-026** `CommandBar` — Wire "Smart Suggestions" section: show the top 3 incomplete items from the AI engine (e.g. "FD maturing in 5 days — renew?", "Budget 92% used", "Goal contribution overdue"). Each is a tappable card that deep-links to the relevant screen.

**T-027** `DashboardScreen` — Long-press on the primary FAB opens `CommandBar` instead of the current action sheet. Short-press still opens QuickEntrySheet. Update FAB tooltip to reflect this.

**T-028** All screens — Add a `⌘` icon button in the top-right `CupertinoNavigationBar` trailing area that also opens `CommandBar`. Use a consistent icon (e.g. `CupertinoIcons.search_circle_fill` or a custom grid icon).

---

### NAV-02 · Restructure Manage Hub

**T-029** `ManageScreen` — Group the flat list into three visual sections with section headers: **Money** (Accounts, Payment Apps, Lending & Borrowing), **Grow** (Investments, Goals, Savings Planners), **Control** (Budgets, Categories, Tags, Contacts, Reports). Use `CupertinoListSection.insetGrouped` for clean visual separation.

**T-030** `ManageScreen` — Add section collapse/expand with `AnimatedContainer` height transition. Store collapsed state per section in SharedPreferences (`manage_section_money_collapsed`).

**T-031** `ManageScreen` — Add a search bar at the top that filters the manage items list in real-time. Searching "budget" jumps straight to the Budgets row. Minimum 2 characters to filter.

---

### NAV-03 · Surface Financial Calendar

**T-032** Add a `CalendarWidget` to the dashboard widget grid (`DashboardController`) as a default-on widget. Shows the next 3 upcoming events (FD maturity, SIP, EMI, goal milestone) in a compact card format with date + event type chip.

**T-033** `FinancialCalendarScreen` — Add an entry point from the Dashboard header area: a small calendar icon badge that shows a red dot when any event is due within 7 days.

**T-034** `AppMenuScreen` — Move Calendar to the top section (currently it's buried). Add a "Today" badge showing event count if any events fall today.

---

### NAV-04 · Smart Quick Access (usage-based)

**T-035** Create `lib/services/usage_tracker_service.dart` — A singleton that records a screen tap count per route name in SharedPreferences (`usage_tap_count_/route`). Expose `topRoutes(int n)` → sorted list of top N routes by tap count.

**T-036** `EngagementStripWidget` (Quick Access strip) — Replace the 3 fixed items with a dynamic list from `UsageTrackerService.topRoutes(4)`. Default to `[Transactions, Budgets, Calendar, Investments]` until 7+ days of usage data exists.

**T-037** `UsageTrackerService` — Call `UsageTrackerService.instance.record(routeName)` from the global navigator observer (`RouteObserver`) so every screen visit is tracked without individual screens needing to call it manually.

---

### NAV-05 · Back Navigation Consistency

**T-038** Audit all screens opened via `showCupertinoModalPopup` vs `Navigator.push`. Any screen that is a full-screen destination (InvestmentsScreen, GoalsScreen, BudgetsScreen) must use `Navigator.push` + `FadeScalePageRoute`, not a modal popup. Modal popups are for sheets/wizards only. Fix any misclassified screens.

**T-039** Add `WillPopScope` (or `PopScope` on Flutter 3.16+) to all multi-step wizards. If user has entered data on any step and tries to back-navigate, show a `CupertinoAlertDialog`: "Discard changes?" with Cancel / Discard. Never silently lose wizard data.

---

## PHASE 2 — Onboarding Overhaul (20 tasks)

---

### ONB-01 · Replace Static Carousel with Activation Flow

**T-040** Create `lib/ui/onboarding/onboarding_activation_screen.dart` — 4-step progress wizard replacing the current 5-page marketing carousel. Steps shown as a segmented progress bar at top.

**T-041** Onboarding Step 1 — "Your first account". Render `AccountWizard` inline (not as a modal). Cannot skip. On account save → animate checkmark + "₹X tracked. You're set." → auto-advance.

**T-042** Onboarding Step 2 — "Import your transactions". Three cards: SMS Scan (one-tap, shows progress), CSV Import (opens `CsvImportScreen`), Add Manually (opens QuickEntrySheet). Skip button bottom-right. After SMS scan completes show "Found N transactions from [bank names]".

**T-043** Onboarding Step 3 — "Set one goal". Simplified goal wizard: name field (with suggestions: Emergency Fund, Europe Trip, New Phone, Home Down Payment), target amount, deadline date picker. Skip button. On save → "Tracked. We'll remind you when you're near."

**T-044** Onboarding Step 4 — "Enable AI insights". Full-bleed card showing the AI scorecard preview (blurred/placeholder). Two bullets: what AI does, what data it needs. Toggle to enable. Skip button. On enable → "Insights unlock after 7 days of transactions."

**T-045** Onboarding completion — Spring-in confetti burst (use `ConfettiWidget` package or a simple particle system with random `AnimatedPositioned` colored dots). Show "Your Financial OS is ready." with a "Let's go" button that pushes `DashboardScreen`.

**T-046** `main.dart` / `SplashScreen` — Update the onboarding routing to check for the new activation completion flag (`onboarding_v2_complete`). Old flag (`onboarding_complete_v1`) skips onboarding as usual.

---

### ONB-02 · Feature Discovery Tooltips

**T-047** Create `lib/services/tooltip_service.dart` — Tracks which tooltips have been shown (`tooltip_shown_N`). Exposes `shouldShow(int id) → bool` and `markShown(int id)`. Uses SharedPreferences.

**T-048** Session 2 tooltip — After second app open, show a `CoachMark`-style overlay on the global search icon: "Search with natural language — try 'food last week'". Dismiss on tap. Use a semi-transparent backdrop with a spotlight cutout.

**T-049** Session 3 tooltip — Show a coach mark on the Quick Entry mic button: "Tap to add transactions by voice — in English, Hindi, or Hinglish." Dismiss on tap.

**T-050** Session 4 tooltip — Show a coach mark on the Calendar icon: "This calendar shows when your FDs mature, SIPs are due, and bills are coming." Dismiss on tap.

**T-051** Session 5 tooltip — Show a full-width dismissable banner below the nav bar: "Did you know? Say 'goals dikhao' or 'show my budget' to navigate hands-free." Dismiss on tap.

**T-052** Create `lib/ui/widgets/coach_mark.dart` — Reusable spotlight overlay widget. Accepts `targetKey` (GlobalKey), title, body text, optional action button. Animates in with a `ScaleTransition` from the target widget's position.

---

### ONB-03 · Empty State CTAs

**T-053** `TransactionHistoryScreen` empty state — Replace generic empty state with: illustration + "No transactions yet" + primary button "Add your first transaction" (calls `QuickEntrySheet.show(context)`).

**T-054** `InvestmentsScreen` empty state — "No investments tracked yet" + two buttons: "Add a Fixed Deposit" (opens FD wizard) and "Add a SIP" (opens SIP wizard).

**T-055** `BudgetsScreen` empty state — "No budgets set" + button "Create a Food budget" (opens budget wizard with Food pre-selected as category).

**T-056** `GoalsScreen` empty state — "No goals yet" + button "Save for something" (opens goal wizard with name field focused).

**T-057** `LendingBorrowingScreen` empty state — "No loans tracked" + button "Add money you lent" and "Add money you borrowed" side by side.

**T-058** `AccountsScreen` empty state — "Add your first account to start tracking" + button "Add Account" (opens AccountWizard). This is the first empty state new users see; make it welcoming, not clinical.

**T-059** `SpendingInsightsScreen` — When < 7 days of data exist: show a progress ring (days out of 7) with text "Insights unlock in N days of transactions. Keep adding!" No empty list, just this encouraging state.

---

## PHASE 3 — UX Smoothness & Polish (30 tasks)

---

### UX-01 · Quick Entry Progressive Disclosure

**T-060** `QuickEntrySheet` — Restructure form into two tiers. Tier 1 (always visible): amount field (auto-focused), description field, transaction type toggle (Expense / Income / Transfer). Tier 2 (collapsed by default): category, tags, merchant, cashback toggle, account selector, split option.

**T-061** `QuickEntrySheet` — Add an expand/collapse chevron arrow at the bottom of Tier 1. On tap: `AnimatedContainer` springs open Tier 2 with `Curves.easeOutCubic` + `duration: 320ms`. The "More" label changes to "Less" when expanded. Persist expansion state to SharedPreferences (`quick_entry_expanded`).

**T-062** `QuickEntrySheet` — When Tier 2 is collapsed and a category/tag has been set, show a compact summary row: e.g. "Food · #lunch · HDFC" so user can see their selections without expanding.

---

### UX-02 · Swipe-to-Delete and Swipe-to-Edit on Transaction Rows

**T-063** `TransactionHistoryScreen` — Wrap each transaction row in `Slidable` (already a dependency). Left swipe reveals: Delete (red, `CupertinoIcons.trash`) + Archive (amber, `CupertinoIcons.archivebox`). Right swipe reveals: Edit (blue, `CupertinoIcons.pencil`) + Duplicate (grey, `CupertinoIcons.doc_on_doc`).

**T-064** `TransactionHistoryScreen` — "Edit" swipe action opens the transaction detail sheet in edit mode. "Duplicate" creates a copy of the transaction with today's date and opens it in edit mode (user confirms before saving).

**T-065** `TransactionHistoryScreen` — "Delete" action triggers a haptic `HapticFeedback.heavy()` and shows an undo toast (see UX-05). Does not immediately delete — adds to the undo queue.

**T-066** `AccountsScreen` transaction rows (inside account detail view) — Apply the same `Slidable` treatment as T-063.

---

### UX-03 · Transaction Undo Queue

**T-067** `TransactionsController` — Add `List<Transaction> _undoQueue = []` (max 5 items). After any `delete()` or `archive()`, push the transaction to `_undoQueue` and start a 5-second `Timer`. If timer fires without an undo call → finalize deletion (already done in DB), clear from queue.

**T-068** `TransactionsController` — Add `Future<void> undoLastDelete()` — pops from `_undoQueue`, re-inserts into DB, calls `notifyListeners()`. If queue is empty, no-op.

**T-069** `ToastNotification` — After any delete/archive, show: "Transaction deleted · **Undo**" with a countdown bar. The "Undo" text is tappable and calls `TransactionsController.undoLastDelete()` then dismisses the toast. Countdown bar depletes over 5 seconds.

**T-070** `TransactionsController` — When app is backgrounded (`WidgetsBindingObserver.didChangeAppLifecycleState → paused`) — finalize all pending undo queue items immediately (commit deletions, clear queue). Prevents zombie deleted items surviving across sessions.

---

### UX-04 · Swipe Gestures on Dashboard Cards

**T-071** `BaseDashboardWidget` — Add long-press + drag gesture to trigger dashboard widget reorder mode. On long-press: widget scales to 1.05x (spring bounce), enters drag mode, other widgets dim to 70% opacity. On drag release: spring-settle into new position, save to `DashboardController`.

**T-072** `DashboardScreen` — Add a subtle "Edit layout" text link in the dashboard header area (small, muted colour) that appears after 3 sessions to hint that the dashboard is customizable. Disappears after user has reordered once.

---

### UX-05 · Increased Tap Targets

**T-073** `FinancialCalendarScreen` — Wrap calendar day cells in `GestureDetector` with minimum `SizedBox(width: 44, height: 44)`. Current cells are likely 32–36pt.

**T-074** `SpendingInsightsScreen` — Category chip pills and filter tabs: ensure minimum height 44pt, minimum horizontal padding 12pt so they're thumb-friendly.

**T-075** `InvestmentsScreen` tab strip pills (the header category tabs) — Minimum height 44pt. Currently 60pt SizedBox so height is fine, but verify horizontal padding gives ≥ 44pt total touch width for short labels like "FD".

**T-076** `ManageScreen` list rows — Ensure all `ListTile`/custom rows have `minVerticalPadding: 12` so minimum row height is 48pt.

---

### UX-06 · Micro-animations & Delight

**T-077** `GoalsScreen` goal card — Animate progress bar fill on first appearance (not instant). Use `AnimationController` + `Tween<double>(begin: 0, end: progress)` with a 600ms ease-out-cubic. Triggers once per session when the card scrolls into view.

**T-078** `BudgetsScreen` budget card — Same animated fill for the budget usage bar. When budget exceeds 80%, the bar color transitions from teal to amber; at 100% it pulses red once using a `RepeatAnimation` for 3 cycles then stops.

**T-079** `NetWorthPage` — Animate the net worth number on load using `AnimatedCounter` (already exists). Also animate the arc/gauge fill from 0 to final value on page enter.

**T-080** `AchievementsScreen` — When an achievement is newly unlocked, show a 1-second particle burst (existing `FloatingParticleBackground` component) centred on the achievement card, then settle. Haptic: `HapticFeedback.success()` pattern.

**T-081** `DashboardScreen` — Add a "Good morning / afternoon / evening, [first name from accounts]" greeting in the header that changes based on time of day. Pull first name from the most-used account name or from a user-set display name in Settings.

---

### UX-07 · Haptic Vocabulary Completeness

**T-082** Audit all `onPressed` callbacks across the app. Any destructive action (delete, archive, cancel wizard) must call `HapticFeedback.heavy()`. Any success action (save, confirm, goal reached) must call the success pattern (2 light taps). Any navigation tap must call `HapticFeedback.light()`. Create a `Haptics` utility class with these presets to ensure consistency.

**T-083** Voice overlay — On mic open: `HapticFeedback.mediumImpact()`. On transcript final: `HapticFeedback.lightImpact()`. On confirmation (save): `HapticFeedback.heavy()` then success pattern. Currently only `lightImpact` on mic open — expand to full vocabulary.

---

### UX-08 · Pull-to-Refresh

**T-084** `TransactionHistoryScreen`, `InvestmentsScreen`, `AccountsScreen` — Add `RefreshIndicator` (or `CupertinoSliverRefreshControl` for native iOS feel) at the top of each list. On pull-to-refresh: re-run `controller.load()`, refresh investment values, re-run AI analysis.

**T-085** `SpendingInsightsScreen` — Pull-to-refresh triggers re-run of `AIIntelligenceController.refresh()` with current data. Show a loading skeleton while refreshing rather than replacing content instantly.

---

### UX-09 · Loading & Skeleton States

**T-086** `InvestmentsScreen` — When `refreshCurrentValues()` is running in the background, show a subtle pulsing skeleton on the "Current Value" column of investment cards instead of showing stale data. Use existing `SkeletonLoader` widget.

**T-087** `SpendingInsightsScreen` — When `AIIntelligenceController` is in `refreshing` state, show skeleton loaders for each insight section rather than the previous result. Prevents showing stale insights as if they're current.

**T-088** `FinancialCalendarScreen` — Show a skeleton event list while the calendar is building its predicted events from AI data. Particularly important on first load when the AI engine is initializing.

---

## PHASE 4 — AI Output Quality (25 tasks)

---

### AI-01 · Narrative Engine — 8 Lens Rotation

**T-089** `AIIntelligenceController` / `MonthlyNarrativeGenerator` — Add `NarrativeLens` enum with 8 values: `biggestSpike`, `goalOnTrack`, `budgetExceeded`, `anomalySpotted`, `savingStreak`, `newHabitDetected`, `investmentMilestone`, `cashflowHealthy`.

**T-090** `MonthlyNarrativeGenerator` — For each lens, write a `_buildXxxNarrative()` method that checks if the data supports that lens. Example: `_buildBiggestSpikeNarrative()` finds the category with the highest MoM % increase and names the top merchant. Return null if data doesn't support the lens.

**T-091** `MonthlyNarrativeGenerator` — `selectLens()` method: try each lens in priority order (anomaly > budget exceeded > goal > saving streak > new habit > spike > investment > healthy). Skip any lens used in the previous month (stored in SharedPreferences `last_narrative_lens`). Return the first supported lens.

**T-092** `MonthlyNarrativeGenerator` — Narrative text must be specific to user data. No generic sentences. Every narrative must include: at least one merchant name, at least one ₹ amount, at least one comparison (vs last month / vs goal / vs average). Write all 8 narrative templates.

**T-093** `AiMonthlyPlannerScreen` — Surface the narrative prominently as the first card. Use a large font (title2) for the first sentence. The rest of the narrative is body text below. Add a "Share" button that copies the narrative to clipboard as plain text.

---

### AI-02 · Health Score Drill-Down

**T-094** `NetWorthPage` / `HealthScoreWidget` — Make each of the 6 health score dimension bars tappable. On tap: spring-in a bottom sheet `HealthScoreDimensionSheet`.

**T-095** Create `lib/ui/widgets/health_score_dimension_sheet.dart` — Shows: dimension name + score (large, animated fill), "Why this score" section (2–3 bullet points with specific data from `HealthScoreDimension.reasons`), "How to improve" section (1 specific action sentence), trend sparkline (last 6 months of this dimension's score as a small line chart).

**T-096** `FinancialHealthScorer` — Each `HealthScoreDimension` object must carry a `List<String> reasons` (populated with specific data, e.g. "Saved in 4 of last 6 months") and a `String improvementTip` (specific action). Wire actual computed data into these fields rather than leaving them empty.

**T-097** `HealthScoreDimensionSheet` — Add a "Trend" mini sparkline using `CustomPainter`. Draw a simple line chart of the last 6 months of this dimension's score. Color: green if trending up, red if trending down, grey if flat.

---

### AI-03 · Opportunity Spotter — Account-Aware

**T-098** `OpportunitySpotter` — Add `_checkFdMaturity()`: if any FD matures within 30 days, generate an opportunity: "Your [bank] FD of ₹X matures on [date]. Tap to plan renewal." Deep-link to the FD details.

**T-099** `OpportunitySpotter` — Add `_checkUnderusedBudget()`: if any budget has been under 50% used for 3 consecutive months, generate: "Your [Budget] budget is barely used. Consider lowering it to ₹X to reduce mental overhead."

**T-100** `OpportunitySpotter` — Add `_checkSubscriptionOverlap()`: detect if the user has both Hotstar and Jio Cinema, or both Spotify and JioSaavn (merchant normalizer identifies these). Generate: "You may be paying for overlapping streaming services: [A] and [B]. Combined: ₹X/month."

**T-101** `OpportunitySpotter` — Add `_checkSalaryDayInvestment()`: if salary is detected (recurring income around month start/end) and no SIP transaction occurs within 3 days of salary, generate: "Consider setting up a SIP on salary day — automating investing before spending."

**T-102** `SpendingInsightsScreen` — Opportunity cards displayed as horizontally scrollable chips at the top of the screen (not buried in a list). Each chip: icon + one-line summary. Tap to expand into a bottom sheet with full detail and action button.

---

### AI-04 · Habit Loop Closure

**T-103** `HabitConstructor` — When a `HabitContract` is confirmed by the user ("Yes, track this"), save it to SharedPreferences with `confirmedAt` timestamp and `weeklyTarget` (derived from the habit's frequency pattern).

**T-104** Create `lib/logic/ai/habit_weekly_checker.dart` — Runs every Sunday. For each confirmed habit: count transactions matching the habit's merchant/category pattern in the last 7 days. Compute `actualCount` vs `weeklyTarget` and `actualSpend` vs `typicalSpend`.

**T-105** `DashboardScreen` — Add a `HabitCheckInWidget` to the dashboard (default hidden, appears after first habit is confirmed). Shows: "Coffee habit: 5/7 days · ₹350 this week". Tap → `HabitDetailSheet` with weekly trend sparkline.

**T-106** Create `lib/ui/widgets/habit_detail_sheet.dart` — Shows the habit's: name, weekly progress (bar), spend trend (sparkline), streak count ("3 weeks logged"), best week, and a "Stop tracking" option.

**T-107** `HabitWeeklyChecker` — After 4 consecutive weeks of a habit being tracked, fire a celebration: push a `NotificationCard` to the notifications page: "You've logged your [habit] for a full month! 🎯" and unlock an achievement badge.

---

### AI-05 · Predictive Calendar Ghost Events

**T-108** `FinancialCalendarScreen` — Add a "Predicted" event type to the event model. Predicted events display with a dashed border, lighter opacity (60%), and a small "Predicted" label chip.

**T-109** `PredictedCalendar` (in `AIIntelligenceController`) — Wire predicted recurring transactions into the calendar event list. Each predicted event carries a `confidenceScore` (0.0–1.0). Only show predictions with confidence > 0.6.

**T-110** `FinancialCalendarScreen` — Tapping a predicted event shows a sheet: "This is a predicted event based on your history. It appears every [frequency]. [Confirm as scheduled] or [Dismiss]." Confirming creates a real calendar event.

**T-111** `FinancialCalendarScreen` — Add a filter toggle row: "Show predicted events" (on by default). Toggling off hides all ghost events. Store preference in SharedPreferences.

---

### AI-06 · Anomaly Explanation

**T-112** `AnomalyDetector` — Each anomaly must carry an `explanation` string: "This ₹8,500 Amazon transaction is 4.2x your average Amazon spend (₹2,020)." Not just "Unusual transaction detected."

**T-113** `SpendingInsightsScreen` anomaly card — Show the `explanation` text. Add two action buttons: "Mark as expected" (dismisses permanently for that merchant+amount range) and "View transaction" (navigates to the transaction detail).

---

## PHASE 5 — Investment P&L & Live Prices (18 tasks)

---

### INV-01 · Live Price Integration

**T-114** `InvestmentValueService` — Add `fetchMfNav(String isin) → Future<double?>`. Hit `https://api.mfapi.in/mf/{schemeCode}` (free, official AMFI data proxy). Cache result with 24-hour TTL in `SharedPreferences` (`nav_cache_{isin}`).

**T-115** `InvestmentValueService` — Add `fetchStockPrice(String symbol) → Future<double?>`. Use the NSE India unofficial endpoint or a free tier of a market data provider. Cache with 15-minute TTL. Gracefully return null if unavailable (offline or market closed).

**T-116** `InvestmentValueService` — Add `fetchCryptoPrice(String coinId) → Future<double?>`. CoinGecko free API: `https://api.coingecko.com/api/v3/simple/price?ids={id}&vs_currencies=inr`. Cache 15-minute TTL.

**T-117** `InvestmentValueService` — Already has `_getCurrentGoldPrice()`. Verify it is wired end-to-end: (a) API call succeeds, (b) result flows into `Investment.currentValue` for Digital Gold type, (c) portfolio total reflects it. Fix any broken links.

**T-118** `InvestmentsController` — After `refreshCurrentValues()` completes, compute `unrealisedPnL = currentValue - investedAmount` and `pnlPercent = (unrealisedPnL / investedAmount) * 100` for each investment. Store on the model. Call `notifyListeners()`.

**T-119** `InvestmentsScreen` investment card — Show unrealised P&L below the current value: "+₹3,200 (+14.2%)" in teal (gain) or "-₹800 (-3.1%)" in red (loss). Use `AnimatedCounter` for the amount. Show "—" if price unavailable.

**T-120** `InvestmentsScreen` portfolio header — Show aggregate unrealised P&L across all investments: "Portfolio: ₹4,82,000 · +₹18,400 (+3.97%)". Color the delta teal/red based on sign.

**T-121** `NetWorthPage` — Investment section must use `currentValue` (live-priced) not `investedAmount`. If live price unavailable for any investment, show `investedAmount` with a "(last known)" footnote.

---

### INV-02 · XIRR Calculation

**T-122** Create `lib/logic/finance/xirr_calculator.dart` — Implement the Newton-Raphson XIRR algorithm. Input: `List<(DateTime date, double cashflow)>`. Output: annualized rate as a double. Handle edge cases: single cashflow (return 0), all positive cashflows (return infinity guard), non-convergence after 100 iterations (return null).

**T-123** `InvestmentsController` — For each investment with a known purchase date and current value, call `XirrCalculator.compute(cashflows)`. Store result as `xirrAnnualised` on the investment. Run in an `Isolate` to avoid blocking the UI (use `compute()` function).

**T-124** Investment detail sheets (FD, MF, Stocks) — Show: "ROI: 18.4% total · XIRR: 14.2% p.a." Side by side. Add an info icon `ⓘ` next to XIRR that shows a tooltip: "XIRR accounts for the timing of each investment — it's the most accurate measure of real returns."

---

### INV-03 · Investment Timeline Chart

**T-125** `InvestmentsScreen` — Add a portfolio value timeline chart at the top (collapsible). X-axis: months. Y-axis: total portfolio value. Uses historical transaction data to reconstruct portfolio value at each month-end. Draw with `CustomPainter` (consistent with existing chart patterns).

**T-126** `InvestmentsScreen` timeline chart — Add two lines: "Invested amount" (flat stepped line, dark) and "Current value" (live-priced smooth line, teal). The gap between them is the P&L zone, colored with a teal/red gradient fill.

---

### INV-04 · FD Maturity Alert

**T-127** `FinancialCalendarScreen` + `NotificationHelpers` — 30 days before any FD matures, push a notification: "Your [Bank] FD of ₹X matures on [date]. Decide whether to renew or withdraw." Deep-link to the FD detail screen.

**T-128** FD detail sheet — Add a "Maturity Action" section that appears when `daysToMaturity < 30`: "What will you do at maturity?" with three options: Auto-renew (sets a flag), Withdraw to account (pre-fills a transfer wizard), Invest elsewhere (opens investment type picker).

---

## PHASE 6 — Security & Trust Completeness (12 tasks)

---

### SEC-01 · Encrypted Backup

**T-129** `BackupRestoreScreen` — Add "Encrypt backup" toggle in the export flow. When enabled, show a password field (confirm password). Derive a 256-bit AES key using PBKDF2 (100,000 iterations, random 16-byte salt). Encrypt the JSON using `encrypt` package.

**T-130** `BackupRestoreScreen` — Encrypted backup file gets `.vittara_enc` extension. Plain backup keeps `.vittara_backup` (JSON). The restore screen detects the extension: if `.vittara_enc`, prompt for password before decrypting.

**T-131** `BackupRestoreScreen` — Show a warning below the unencrypted export option: "This file contains all your financial data. Store it securely." For the encrypted option: "Password-protected. Keep your password safe — we cannot recover it."

---

### SEC-02 · Recovery Code Regeneration

**T-132** `SettingsScreen` Security section — Add "Recovery Code" row with a `CupertinoListTile`. Shows last-generated date. Tapping requires biometric auth.

**T-133** Create `RecoveryCodeScreen` — After biometric auth: shows current code (blurred by default, tap eye icon to reveal for 10 seconds). "Regenerate" button with confirmation dialog: "Old code will no longer work." On confirm: generate new 16-char alphanumeric code, store in secure storage, invalidate old code.

---

### SEC-03 · Biometric Re-auth for Sensitive Actions

**T-134** `TransactionsArchiveScreen` — Gate entry with biometric re-auth. On screen push, before rendering content, call `LocalAuthentication.authenticate()`. If auth fails, pop the screen with a "Authentication required" toast.

**T-135** `BackupRestoreScreen` export action — Before generating the backup file, require biometric re-auth.

**T-136** `RecoveryCodeScreen` — Already biometric-gated via T-132. Ensure the auth prompt shows a meaningful `localizedReason`: "Authenticate to view your recovery code".

**T-137** `SettingsScreen` — Add a "Require biometric for sensitive screens" master toggle. When on: archive, backup export, and recovery code require auth. When off: these screens are accessible with just the app lock (PIN/biometric on open).

---

### SEC-04 · Device Security UX

**T-138** `DeviceSecurityService` — If device is rooted: show a one-time warning `CupertinoAlertDialog` explaining that data security may be compromised on rooted devices. Store `security_warning_shown` flag so it's shown only once, not on every launch.

**T-139** `SettingsScreen` — Add a "Security Status" row that shows: ✓ Encrypted database / ✓ Biometric enabled / ✓ Screenshot protection / ⚠ Rooted device detected (if applicable). Green checkmarks or amber warnings.

---

## PHASE 7 — Personalization Engine (15 tasks)

---

### PERS-01 · Usage-Based Dashboard Reorder

**T-140** `UsageTrackerService` (from T-035) — After 14 days of usage, compute the most-used dashboard widgets by tracking which widget cards are tapped most. Offer a one-time "Optimise my dashboard" prompt on day 14.

**T-141** `DashboardController` — Add `reorderByUsage(Map<String, int> tapCounts)` method. Sorts the widget grid by tap frequency descending. Preserves column span. Saves new order via the existing migration system (increment widget migration version).

---

### PERS-02 · Behavioural Quick Entry Pre-fill

**T-142** `QuickEntrySheet` — On open, call `BehaviourPredictor.predictContext(DateTime.now())`. Returns `{category, merchant, amount}` based on time-of-day + day-of-week patterns from the last 90 days of transactions. Pre-fill these fields as placeholder (not forced value — user can override).

**T-143** `BehaviourPredictor` — `predictContext()` method: group past transactions by `(hour_bucket, day_of_week)`. For the current bucket, find the most common `(category, merchant)` pair. If confidence > 0.7, return as prediction. Otherwise return null (no pre-fill).

**T-144** `QuickEntrySheet` — Show pre-filled values with a distinct muted style (grey text) with a "Based on your habits" tooltip (ⓘ) so the user knows it's a suggestion, not mandatory.

---

### PERS-03 · Smart Notification Timing

**T-145** Create `lib/services/usage_timing_service.dart` — Records `app_open_hour_histogram` as a 24-bucket int array in SharedPreferences. Increments the bucket for the current hour on every app open. Exposes `peakHours() → List<int>` (top 2 hours by frequency).

**T-146** `NotificationHelpers` — When scheduling budget warning / AI insight notifications, use `UsageTimingService.peakHours().first` as the notification hour instead of a fixed time. Update notification scheduling logic accordingly.

---

### PERS-04 · Per-User Display Name & Greeting

**T-147** `SettingsScreen` — Add "Display Name" text field at the top of the profile section. Stored in SharedPreferences. Default: empty (uses "there" as fallback in greeting).

**T-148** `DashboardScreen` header — Add greeting: "Good morning, [name]" / "Good afternoon, [name]" / "Good evening, [name]". Time-based selection: morning before 12, afternoon 12–17, evening after 17. Font: `title2` size, primary text colour. Positioned above the net worth summary.

---

### PERS-05 · Theme Customisation Extension

**T-149** `SettingsScreen` — Add "Accent Colour" picker. Offer 6 preset accents: Aether Teal (current), Nova Purple, Solar Gold, Coral Red, Sky Blue, Mint Green. Changing accent updates `AppStyles.primaryColor` which flows through all themed elements. Store in SharedPreferences.

**T-150** `SettingsController` — Add `accentColor` property. Expose a `ThemeData copyWith` that replaces the primary swatch with the selected accent. Wire through `MaterialApp.theme` in `main.dart`.

---

### PERS-06 · Default Currency & Number Format

**T-151** `SettingsScreen` — Add "Number format" toggle: Indian (1,00,000) vs International (100,000). Default: Indian. Affects all `_fmt()` calls throughout the app. Store in SharedPreferences `number_format_indian`.

**T-152** Create `lib/utils/currency_formatter.dart` — Centralise all currency formatting. `formatINR(double amount, {bool short = false})` respects the `number_format_indian` setting. Replace direct `NumberFormat` calls throughout the app with this utility.

---

## PHASE 8 — Performance & Reliability (20 tasks)

---

### PERF-01 · Transaction List Pagination

**T-153** `TransactionsController` — Add `loadPage(int page, {int pageSize = 50})` that queries the DB with `LIMIT 50 OFFSET (page * 50)`. Add `hasMore` bool and `currentPage` int to state.

**T-154** `TransactionHistoryScreen` — Replace `controller.transactions` (full list) with a paginated `ListView.builder` that triggers `controller.loadPage(nextPage)` when the user scrolls within 200px of the bottom (`ScrollController.position.pixels > maxScrollExtent - 200`).

**T-155** `TransactionHistoryScreen` — Show a `SkeletonLoader` row at the bottom of the list while the next page is loading. Remove once page arrives. Never show a spinner that blocks the existing content.

**T-156** `TransactionsController` — Search/filter operations on lists > 500 items: run in `Isolate` via `compute()`. `_filterTransactions(FilterArgs args)` as a top-level function. Pass the full list + filter params, return filtered list. UI shows skeleton while isolate runs.

---

### PERF-02 · Startup Time Reduction

**T-157** `main.dart` — Move all non-critical controller initialisation to post-first-frame. Only `TransactionsController`, `AccountsController`, and `SettingsController` need to be ready before the first build. All other controllers (Insurance, Loans, Contacts, Tags, Categories, etc.) can `load()` in a `Future.microtask` after `addPostFrameCallback`.

**T-158** `DashboardController` — The 9-migration chain runs on every cold start. Add a `migrationVersion` check at the top: if `storedVersion == currentVersion`, skip all migrations immediately. This cuts startup time for returning users.

**T-159** `SplashScreen` — Current minimum wait is 1200ms. Reduce to 800ms. The extra 400ms adds no value once the DB is open and controllers are initialised.

---

### PERF-03 · Improved Duplicate Detection

**T-160** `SmsAutoScanService` — Replace the ±₹1 / ±1 day duplicate window with: exact amount match + normalised merchant match (using `MerchantNormalizer`) + account match + ±6 hour window. Also add SMS body hash check: if the same SMS body has already been processed, skip regardless of amount/date.

**T-161** `SmsAutoScanService` — Store `Set<String> _processedSmsHashes` in SharedPreferences (`sms_processed_hashes`, max 1000 entries, evict oldest when full). Check before processing any SMS.

---

### PERF-04 · Memory & Scroll Performance

**T-162** `InvestmentsScreen` investment card — Wrap each card in `RepaintBoundary` to isolate repaints. The P&L animation (T-119) updates per card independently; without `RepaintBoundary` the entire list repaints on each card's animation frame.

**T-163** `DashboardScreen` — Each dashboard widget is independently animated. Add `RepaintBoundary` around each `BaseDashboardWidget` instance. Confirmed effective for the `AnimatedCounter` in the net worth widget.

**T-164** `TransactionHistoryScreen` — Use `const` constructors for the separator widget (`Divider`) and the date header widget (they never change). Small but cumulative improvement across large lists.

---

### PERF-05 · Database Query Optimisation

**T-165** `DatabaseService` — Add a compound index on `(source_account_id, date_time)` for the transactions table. The account detail view queries transactions by account — this index makes it O(log n) instead of O(n).

**T-166** `DatabaseService` — Add an index on `(type, date_time)` for the transactions table. The insights engine frequently queries by type + date range. This eliminates full table scans on that path.

---

### QA-01 · Edge Case Hardening

**T-167** `TransactionsController` — Handle the "deleted account" case: if `sourceAccountId` refers to an account that no longer exists, the transaction should display "Deleted account" as the account name rather than crashing or showing null. Already stored as `metadata['accountDeleted']` — ensure the UI reads this correctly everywhere.

**T-168** `GoalsController` — Handle negative `daysRemaining` (goal deadline passed). Currently might show "−45 days remaining" which is confusing. Show "Overdue by 45 days" in amber, not a negative number.

**T-169** `BudgetsController` — Period rollover edge case: if the app is not opened for 2+ budget periods, the budget should roll over multiple times on next open, not just once. Fix `_checkAndRollover()` to loop until the period is current, not just one step.

**T-170** `FdWizardController` — If maturity date is in the past (user adding a historical FD), show a warning: "This FD has already matured. Consider adding the maturity proceeds as income instead." Don't block saving, just warn.

**T-171** `LoanWizard` — If EMI amount × tenure < principal (impossible amortization), show a validation error inline rather than allowing a nonsensical loan to be saved.

**T-172** `VoiceFillEngine` — If `processAsync()` is called while a previous call is still in-flight (rapid double-tap on mic), cancel the previous call before starting a new one. Add an `_inFlight` flag and check it at the top of `processAsync`.

---

## PHASE 9 — Accessibility & Polish (20 tasks)

---

### A11Y-01 · Screen Reader Labels

**T-173** `InvestmentsScreen` investment cards — Add `Semantics(label: '${inv.name}, ${inv.getTypeLabel()}, current value ₹${_fmt(inv.currentValue)}, gain ${pnl}%')` to each card. The current card has no semantic label so VoiceOver/TalkBack reads random child text.

**T-174** `FinancialCalendarScreen` — Each calendar day cell: `Semantics(label: '${day} ${month}, ${eventCount} events')`. Event dots currently have no semantic meaning.

**T-175** `NetWorthPage` arc chart — `Semantics(label: 'Financial health score: ${score} out of 100, ${label}')` wrapping the `CustomPainter` arc. Currently inaccessible to screen readers.

**T-176** `SpendingInsightsScreen` pie/bar charts — Wrap each chart in `Semantics(label: 'Category breakdown chart')` and add an `ExcludeSemantics` on the visual chart itself. Below each chart, add a visually hidden `Semantics(label: 'Top category: Food at 34%, Transport at 22%...')` for screen reader access.

**T-177** All icon-only buttons (mic button, search icon, close X, etc.) — Add `Tooltip` widget wrapping them with descriptive text. `Tooltip` both shows a label on long-press (accessibility) and feeds the semantic label.

---

### A11Y-02 · Text Scaling

**T-178** `QuickEntrySheet` — Test at 200% text scale (accessibility setting). Amount field must not clip. Action buttons must not overflow. Tier 2 expansion must still work. Fix any overflow using `Flexible` / `Expanded` wrappers.

**T-179** `DashboardScreen` dashboard widgets — Each widget must be tested at 150% text scale. The compact net worth widget and the habit strip widget are most likely to clip. Use `FittedBox` for monetary amounts that must fit in a fixed-size cell.

---

### POLISH-01 · Consistent Empty States

**T-180** Audit all empty states across the app. Every screen that can be empty must have: (1) an illustration (SVG icon, not a photo), (2) a title (bold, 18pt), (3) a subtitle (muted, 14pt explaining why it's empty), (4) a single primary CTA button. No empty states may be blank white/black screens.

**T-181** `TransactionsArchiveScreen` empty state — "Your archive is clean. Archived transactions appear here." + an archive box illustration. No CTA (archive is passive).

---

### POLISH-02 · WhatsNew & Changelog

**T-182** `WhatsNewSheet` — After each major update (detected by comparing stored `app_version` in SharedPreferences vs current `PackageInfo.version`), show the WhatsNew sheet on first launch post-update. Cards must describe new features in user language, not developer language. Write cards for Phase 0–4 changes once they ship.

**T-183** `SettingsScreen` — Add "What's New" row that opens `WhatsNewSheet` on demand so users can revisit the changelog.

---

### POLISH-03 · Dark Mode Completeness

**T-184** Audit all screens in light mode. Some secondary text uses hardcoded `Colors.grey` instead of `AppStyles.getSecondaryTextColor(context)`. Find and replace all hardcoded colors with semantic color tokens. Run a grep for `Colors.grey`, `Colors.black`, `Colors.white` in UI files and replace each with the appropriate token.

**T-185** `ReceiptScannerScreen` and `StatementImportScreen` — These OCR screens currently have a lighter, more utilitarian visual style. Bring them in line with the app's design language: dark card backgrounds, teal accent, Space Grotesk headers.

---

### POLISH-04 · Transition Consistency

**T-186** Audit all `Navigator.push` calls. Any push that isn't using `FadeScalePageRoute` or `CupertinoPageRoute` — standardize. Investment wizard steps should use `CupertinoPageRoute` (slide) for within-wizard navigation. Screen-to-screen should use `FadeScalePageRoute`.

**T-187** All bottom sheets (`showCupertinoModalPopup`) — Add `barrierColor: Colors.black.withValues(alpha: 0.6)` consistently. Currently some sheets use 0.4, some 0.75, some default. Pick one value and apply everywhere.

---

### POLISH-05 · Typography Audit

**T-188** Run a codebase-wide audit for any `fontSize` hardcoded as a literal number (e.g. `fontSize: 16`) outside of `design_tokens.dart` / `RT.*` scale functions. Replace each with the appropriate `RT.body(context)`, `RT.headline(context)`, etc. call so text scales with device settings.

**T-189** Verify `Space Grotesk` is applied to all header/title text and `Plus Jakarta Sans` to all body text across every screen added since the font setup. Grep for `TextStyle(` without `fontFamily` parameter in screen files — these inherit from the theme but explicit overrides may have slipped through.

---

### POLISH-06 · App Icon & Splash Consistency

**T-190** `SplashScreen` — The logo animation on splash should use the same spring physics as the rest of the app (currently likely a simple `FadeTransition`). Change to a scale-up spring: `0.7 → 1.0` with `BouncySpring` preset, fade from 0 → 1 simultaneously.

---

## PHASE 10 — World-Class Differentiators (10 tasks)
*These push the score from 9.5 → 9.8 by adding capabilities no other Indian personal finance app has.*

---

**T-191** **Annual Financial Review** — On January 1st (or first app open in January), show a full-screen "Your [Year] in Money" summary: total income, total expenses, total invested, net worth change, best savings month, biggest expense category, number of transactions. Shareable as an image (use `RenderRepaintBoundary` to capture as PNG). This is the Spotify Wrapped moment for the app.

**T-192** **Smart Split Detection** — In QuickEntrySheet, if the description contains "split", "divided", "each", or the amount is an unusual denomination (like ₹437.50), show a "Split expense?" card. User enters number of people, the app creates a note in the transaction metadata and optionally creates a lending entry for the other parties.

**T-193** **Tax Harvesting Alert** — For equity investments held > 1 year with unrealised gains > ₹1 lakh: show a nudge in October: "You have ₹X in long-term gains. Consider booking profits before March 31 to use your ₹1L LTCG exemption." This is a genuinely valuable, time-sensitive insight no other app provides proactively.

**T-194** **EMI Affordability Calculator** — In `LoanWizard`, before the user confirms a loan: show "EMI Impact" — "This EMI of ₹X will be Y% of your average monthly income (₹Z). Recommended maximum: 40%." Color-coded: green < 30%, amber 30–40%, red > 40%.

**T-195** **Bill Detection from SMS** — `SmsAutoScanService` currently imports transactions. Extend it to also detect utility bills (electricity, water, gas, internet) from SMS and add them as calendar events + budget entries automatically, even if they're not payment-confirmed SMS.

**T-196** **Savings Round-Up Tracker** — For every expense transaction, compute the "round-up" to the nearest ₹50 (e.g. ₹83 → round-up is ₹17). Show a weekly total: "You could have saved ₹340 this week by rounding up your spends." One-tap to log it as a goal contribution.

**T-197** **Cross-Account Cash Flow View** — A new "Cash Flow" screen (separate from insights) that shows money moving between accounts: salary credited to HDFC → SIP debited → rent paid → Swiggy charged. A Sankey-style diagram showing net flow direction between income sources and expense categories for the month.

**T-198** **Investment Rebalancing Alert** — For users with multiple investment types: compute target allocation vs actual allocation. If any asset class drifts > 10% from target: "Your portfolio is 68% equity vs 60% target. Consider adding ₹X to debt to rebalance." Requires user to set target allocation first (one-time setup in Investments screen).

**T-199** **Voice-Activated Reports** — Extend `VoiceNavigator` and `VoiceFillEngine` to handle report queries: "How much did I spend on food last month?", "What's my savings rate?", "How much do I have in FDs?". The AI answers with TTS and shows the result card on screen. This makes the app feel like a financial assistant, not just a tracker.

**T-200** **Widget (Home Screen)** — Add an Android home screen widget (using `home_widget` package) showing: current month spend vs budget, net worth, and next upcoming calendar event. Updates every 4 hours. This is a constant presence on the home screen that drives daily engagement without requiring app opens.

---

## IMPLEMENTATION ORDER (Recommended Batches)

| Batch | Tasks | Theme | Why this order |
|---|---|---|---|
| **Batch 1** | T-001 to T-022 | All 3 critical bugs | Trust-breaking issues — nothing else matters until these are fixed |
| **Batch 2** | T-063 to T-070 | Swipe-delete + undo | Table-stakes UX, high daily impact |
| **Batch 3** | T-040 to T-058 | Onboarding overhaul | Biggest lever for first-session retention |
| **Batch 4** | T-023 to T-039 | Navigation restructure | Reduces friction for existing users |
| **Batch 5** | T-060 to T-062, T-077 to T-085 | UX polish | Smoothness improvements users feel every day |
| **Batch 6** | T-089 to T-113 | AI output quality | Makes intelligence feel real, not template |
| **Batch 7** | T-114 to T-128 | Live prices + XIRR | Biggest feature credibility gap |
| **Batch 8** | T-129 to T-139 | Security completeness | Trust and safety |
| **Batch 9** | T-140 to T-172 | Personalization + performance | App feels alive and fast |
| **Batch 10** | T-173 to T-190 | Accessibility + polish | World-class finish |
| **Batch 11** | T-191 to T-200 | Differentiators | 9.5 → 9.8 |

---

## FINAL SCORE PROJECTION

| Dimension | Now | After Batch 1–3 | After All 200 |
|---|---|---|---|
| Idea & Vision | 9.2 | 9.2 | 9.5 |
| Feature Depth | 9.0 | 9.0 | 9.8 |
| AI Intelligence | 7.5 | 7.5 | 9.8 |
| UX Smoothness | 7.0 | 8.5 | 9.8 |
| Navigation / IA | 6.5 | 7.5 | 9.5 |
| Personalization | 8.0 | 8.0 | 9.8 |
| Unified Experience | 7.8 | 9.0 | 9.8 |
| Security & Trust | 9.0 | 9.0 | 9.8 |
| Onboarding | 6.0 | 9.0 | 9.5 |
| Performance | 8.0 | 8.0 | 9.8 |
| Reliability | 7.5 | 9.0 | 9.8 |
| **Overall** | **7.8** | **8.7** | **9.8** |

---

*Plan created: 24 April 2026, 00:18*
*200 tasks across 10 phases. Batch 1 (T-001 to T-022) should be the first implementation session.*
*Supersedes: PLAN_2026-04-20_2357.md*
